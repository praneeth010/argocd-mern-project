kubectl top nodes

kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml

kubectl edit deployment metrics-server -n kube-system

edit 

containers:
- args:
  - --cert-dir=/tmp
  - --secure-port=10250
  - --kubelet-preferred-address-types=InternalIP,ExternalIP,Hostname
  - --kubelet-use-node-status-port
  - --metric-resolution=15s
  
change 

containers:
- args:
  - --cert-dir=/tmp
  - --secure-port=10250
  - --kubelet-insecure-tls   # 👈 ADD THIS LINE HERE
  - --kubelet-preferred-address-types=InternalIP,ExternalIP,Hostname
  - --kubelet-use-node-status-port
  - --metric-resolution=15s
  
  
  
  
edit file 

i

after edit this file

💾 After Adding
Save and exit:
Press ESC
Type :wq
Press Enter



Restart metrics-server:

kubectl rollout restart deployment metrics-server -n kube-system

kubectl top nodes


kubectl get nodes

✅ Your node is ALREADY a worker node

Since:

It is not control-plane
It is Ready

👉 It is already acting as a worker node


kubectl label node node01 node-role.kubernetes.io/worker=worker

kubectl get nodes



installing the ArgoCD 

Step by step Giude

kubectl create namespace argocd

kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml


kubectl get pods -n argocd

👉 Wait until all:

Running (1/1)


🔵 Step 4: Enable insecure mode via ConfigMap

kubectl edit configmap argocd-cmd-params-cm -n argocd


✍️ Add this at the bottom

Move cursor to empty space and add:

data:
  server.insecure: "true"
✅ Final file should look like this
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
  namespace: argocd

data:
  server.insecure: "true"
  
  
💾 Save
ESC → :wq → Enter


kubectl rollout restart deployment argocd-server -n argocd

kubectl port-forward svc/argocd-server -n argocd 8089:80 --address 0.0.0.0

geting password ArgoCD

kubectl get secret argocd-initial-admin-secret -n argocd -o jsonpath="{.data.password}" | base64 -d && echo




ArgoCD Setup Dashboard

🚀 Step-by-Step: Create Argo CD Application (Your Screens)
🟢 Step 1: GENERAL Section

Fill like this 👇

Application Name →
mern-app
Project Name →
default
Sync Policy →
👉 Select Automatic (recommended)
✅ Tick:
✔ Auto-create namespace
✔ Prune
✔ Self Heal
🟢 Step 2: SOURCE (VERY IMPORTANT)

👉 You already opened this section

Fill exactly 👇

🔹 Repository URL

Paste your GitHub repo:

https://github.com/praneeth010/argocd-mern-project.git
🔹 Revision

Keep default:

HEAD
🔹 Path (IMPORTANT)

👉 Since your YAML files are in root:

.

(If files were inside folder → give folder name)

🟢 Step 3: DESTINATION

Fill this 👇

🔹 Cluster URL
https://kubernetes.default.svc
🔹 Namespace
mern
🟢 Step 4: DIRECTORY Section

👉 Leave everything default ✅

(No changes needed)

🟢 Step 5: CREATE

👉 Click:

CREATE


